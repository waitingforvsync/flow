MACRO SAMEPAGE addr
    IF HI(addr)<>HI(P%)
        ERROR "Crosses page boundary"
    ENDIF
ENDMACRO

MACRO PCSKIP1
    EQUB &24    ; BIT zp
ENDMACRO

MACRO PCSKIP2
    EQUB &2C    ; BIT abs
ENDMACRO
